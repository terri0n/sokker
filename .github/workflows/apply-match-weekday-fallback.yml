name: Apply match week day fallback

on:
  push:
    branches:
      - fix/update-json-only

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/update-json-only

      - name: Reject unresolvable week and day
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          path = Path('sokker/src/com/formulamanager/sokker/auxiliares/SokkerXmlCompat.java')
          text = path.read_text(encoding='utf-8')
          old = '\t\t\t\tif (week == null || day == null) {\n\t\t\t\t\tcontinue;\n\t\t\t\t}'
          new = '\t\t\t\tif (week == null || day == null) {\n\t\t\t\t\treturn null;\n\t\t\t\t}'
          count = text.count(old)
          if count != 1:
              raise SystemExit('Unexpected week/day target count: %d' % count)
          path.write_text(text.replace(old, new), encoding='utf-8', newline='')
          PY

      - name: Commit fix
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'Terrion'
          git config user.email '63014185+terri0n@users.noreply.github.com'
          git add sokker/src/com/formulamanager/sokker/auxiliares/SokkerXmlCompat.java
          git diff --cached --check
          git commit -m 'Fallback when JSON match week or day is missing'
          git push origin HEAD:fix/update-json-only
